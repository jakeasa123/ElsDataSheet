regexKeywordDict = new Map([
    [/(\d+)제련당/, (match) => `精煉值每 + ${match[1]} 時，`],
    [/(\d+)제련 당/, (match) => `精煉值每 + ${match[1]} 時，`],
    [/(\d+)제련 시/, (match) => `精煉值達 + ${match[1]} 以上時，`],
    [/\[([A-D])등급\]/, (match) => `[${match[1]} 階級] `],

    [/효과 지속시간이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 증가됩니다./,
        (match) => `效果持續時間自 ${match[1]} 秒，增加為 ${match[3]} 秒。`],
    [/효과 지속시간이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 감소됩니다./,
        (match) => `效果持續時間自 ${match[1]} 秒，減少為 ${match[3]} 秒。`],

    [/고정 캐스팅이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 증가됩니다./,
        (match) => `固定詠唱時間自 ${match[1]} 秒，增加為 ${match[3]} 秒。`],
    [/고정 캐스팅이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 감소됩니다./,
        (match) => `固定詠唱時間自 ${match[1]} 秒，減少為 ${match[3]} 秒。`],

    [/변동 캐스팅 (\d+(\.\d+)?)초가 제거됩니다./,
        (match) => `刪除 ${match[1]} 秒的變動詠唱時間。`],
    [/변동 캐스팅이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 증가됩니다./,
        (match) => `變動詠唱時間自 ${match[1]} 秒，增加為 ${match[3]} 秒。`],
    [/변동 캐스팅이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 감소됩니다./,
        (match) => `變動詠唱時間自 ${match[1]} 秒，減少為 ${match[3]} 秒。`],

    [/(\d+)레벨 기준 스킬 쿨타임이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 증가됩니다./,
        (match) => `以等級 ${match[1]} 為基準，冷卻時間自 ${match[2]} 秒，增加為 ${match[4]} 秒。`],
    [/(\d+)레벨 기준 스킬 쿨타임이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 감소됩니다./,
        (match) => `以等級 ${match[1]} 為基準，冷卻時間自 ${match[2]} 秒，減少為 ${match[4]} 秒。`],
        
    [/글로벌 쿨타임 (\d+(\.\d+)?)초가 추가됩니다./,
        (match) => `增加 ${match[1]} 秒的共通技能後延遲。`],
    [/글로벌 쿨타임 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 증가됩니다./,
        (match) => `共通技能後延遲自 ${match[1]} 秒，增加為 ${match[3]} 秒。`],
    [/글로벌 쿨타임 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 감소됩니다./,
        (match) => `共通技能後延遲自 ${match[1]} 秒，減少為 ${match[3]} 秒。`],
    [/글로벌 쿨타임이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 증가됩니다./,
        (match) => `共通技能後延遲自 ${match[1]} 秒，增加為 ${match[3]} 秒。`],
    [/글로벌 쿨타임이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 감소됩니다./,
        (match) => `共通技能後延遲自 ${match[1]} 秒，減少為 ${match[3]} 秒。`],

    [/(\d+)레벨 기준 글로벌 쿨타임이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 증가됩니다./,
        (match) => `以等級 ${match[1]} 為基準，共通技能後延遲自 ${match[2]} 秒，增加為 ${match[4]} 秒。`],
    [/(\d+)레벨 기준 글로벌 쿨타임이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 감소됩니다./,
        (match) => `以等級 ${match[1]} 為基準，共通技能後延遲自 ${match[2]} 秒，減少為 ${match[4]} 秒。`],

    [/(\d+)레벨 기준 글로벌 쿨타임 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 증가됩니다./,
        (match) => `以等級 ${match[1]} 為基準，共通技能後延遲自 ${match[2]} 秒，增加為 ${match[4]} 秒。`],
    [/(\d+)레벨 기준 글로벌 쿨타임 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 감소됩니다./,
        (match) => `以等級 ${match[1]} 為基準，共通技能後延遲自 ${match[2]} 秒，減少為 ${match[4]} 秒。`],

    [/스킬 쿨타임이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 증가됩니다./,
        (match) => `冷卻時間自 ${match[1]} 秒，增加為 ${match[3]} 秒。`],
    [/스킬 쿨타임이 (\d+(\.\d+)?)초에서 (\d+(\.\d+)?)초로 감소됩니다./,
        (match) => `冷卻時間自 ${match[1]} 秒，減少為 ${match[3]} 秒。`],

    [/(\d+)레벨 기준 기본 공격력이 (ATK|MATK) (\d+)%에서 (ATK|MATK) (\d+)%로 증가됩니다./,
        (match) => `以等級 ${match[1]} 為基準，技能倍率自 ${match[2]} ${match[3]}%，增加為 ${match[4]} ${match[5]}%。`],
    [/(\d+)레벨 기준 기본 공격력이 (ATK|MATK) (\d+)%에서 (ATK|MATK) (\d+)%로 감소됩니다./,
        (match) => `以等級 ${match[1]} 為基準，技能倍率自 ${match[2]} ${match[3]}%，減少為 ${match[4]} ${match[5]}%。`],

    [/(\d+)레벨 기준 기본 공격력이 (ATK|MATK) (\d+)%\/(\d+)%에서 (ATK|MATK) (\d+)%\/(\d+)%로 증가됩니다./,
        (match) => `以等級 ${match[1]} 為基準，技能倍率自 ${match[2]} ${match[3]}%/${match[4]}%，增加為 ${match[5]} ${match[6]}%/${match[7]}%。`],
    [/(\d+)레벨 기준 기본 공격력이 (ATK|MATK) (\d+)%\/(\d+)%에서 (ATK|MATK) (\d+)%\/(\d+)%로 감소됩니다./,
        (match) => `以等級 ${match[1]} 為基準，技能倍率自 ${match[2]} ${match[3]}%/${match[4]}%，減少為 ${match[5]} ${match[6]}%/${match[7]}%。`],

    [/(\d+)레벨 기준 기본 1회당 공격력이 (ATK|MATK) (\d+)%에서 (ATK|MATK) (\d+)%로 증가됩니다./,
        (match) => `以等級 ${match[1]} 為基準，每 1 次打擊的技能倍率自 ${match[2]} ${match[3]}%，增加為 ${match[4]} ${match[5]}%。`],
    [/(\d+)레벨 기준 기본 1회당 공격력이 (ATK|MATK) (\d+)%에서 (ATK|MATK) (\d+)%로 감소됩니다./,
        (match) => `以等級 ${match[1]} 為基準，每 1 次打擊的技能倍率自 ${match[2]} ${match[3]}%，減少為 ${match[4]} ${match[5]}%。`],

    [/(\d+)레벨 기준 기본 공격력이 (ATK|MATK) (\d+)%\/(\d+)%\(([^)]+)\)에서/,
        (match) => `以等級 ${match[1]} 為基準，技能倍率自 ${match[2]} ${match[3]}%/${match[4]}%(${match[5]})，`],
    [/(\d+)레벨 기준 기본 1회당 공격력이 (ATK|MATK) (\d+)%\/(\d+)%\(([^)]+)\)에서/,
        (match) => `以等級 ${match[1]} 為基準，技能倍率自 ${match[2]} ${match[3]}%/${match[4]}%(${match[5]})，`],
    [/(ATK|MATK) (\d+)%\/(\d+)%\(([^)]+)\)로 증가됩니다./,
        (match) => `增加為 ${match[1]} ${match[2]}%/${match[3]}%(${match[4]})。`],
    [/(ATK|MATK) (\d+)%\/(\d+)%\(([^)]+)\)로 증가됩니다./,
        (match) => `增加為 ${match[1]} ${match[2]}%/${match[3]}%(${match[4]})。`],

    [/(\d+)레벨 기준 기본 공격력이 (ATK|MATK) (\d+)%\/(\d+)\(([^)]+)\)%에서/,
        (match) => `以等級 ${match[1]} 為基準，技能倍率自 ${match[2]} ${match[3]}%/${match[4]}%(${match[5]})，`],
    [/(\d+)레벨 기준 기본 1회당 공격력이 (ATK|MATK) (\d+)%\/(\d+)\(([^)]+)\)%에서/,
        (match) => `以等級 ${match[1]} 為基準，每 1 次打擊的技能倍率自 ${match[2]} ${match[3]}%/${match[4]}%(${match[5]})，`],
    [/(ATK|MATK) (\d+)%\/(\d+)\(([^)]+)\)%로 증가됩니다./,
        (match) => `增加為 ${match[1]} ${match[2]}%/${match[3]}%(${match[4]})。`],
    [/(ATK|MATK) (\d+)%\/(\d+)\(([^)]+)\)%로 증가됩니다./,
        (match) => `增加為 ${match[1]} ${match[2]}%/${match[3]}%(${match[4]})。`],

    [/에 따른 공격력 증가 비율이 (\d+)에서 (\d+)로 증가됩니다./,
        (match) => ` 的技能倍率係數自 ${match[1]} 增加為 ${match[2]}。`],
    [/에 따른 공격력 증가 비율이 (\d+)에서 (\d+)로 감소됩니다./,
        (match) => ` 的技能倍率係數自 ${match[1]} 減少為 ${match[2]}。`],
    [/에 따른 공격력 증가 비율이 (\d+)에서 (\d+)으로 증가됩니다./,
        (match) => ` 的技能倍率係數自 ${match[1]} 增加為 ${match[2]}。`],
    [/에 따른 공격력 증가 비율이 (\d+)에서 (\d+)으로 감소됩니다./,
        (match) => ` 的技能倍率係數自 ${match[1]} 減少為 ${match[2]}。`],

    [/AP 회복량이 (\d+)에서 (\d+)로 증가됩니다./,
        (match) => `恢復 AP 自 ${match[1]} 增加為 ${match[2]}。`],
    [/AP 회복량이 (\d+)에서 (\d+)로 감소됩니다./,
        (match) => `恢復 AP 自 ${match[1]} 減少為 ${match[2]}。`],
    [/AP 회복량이 (\d+)에서 (\d+)으로 증가됩니다./,
        (match) => `恢復 AP 自 ${match[1]} 增加為 ${match[2]}。`],
    [/AP 회복량이 (\d+)에서 (\d+)으로 감소됩니다./,
        (match) => `恢復 AP 自 ${match[1]} 減少為 ${match[2]}。`],

    [/소모 AP가 (\d+)에서 (\d+)로 증가됩니다./,
        (match) => `消耗 AP 自 ${match[1]} 增加為 ${match[2]}。`],
    [/소모 AP가 (\d+)에서 (\d+)로 감소됩니다./,
        (match) => `消耗 AP 自 ${match[1]} 減少為 ${match[2]}。`],
    [/소모 AP가 (\d+)에서 (\d+)으로 증가됩니다./,
        (match) => `消耗 AP 自 ${match[1]} 增加為 ${match[2]}。`],
    [/소모 AP가 (\d+)에서 (\d+)으로 감소됩니다./,
        (match) => `消耗 AP 自 ${match[1]} 減少為 ${match[2]}。`],
    [/AP 소모량이 (\d+)에서 (\d+)로 증가됩니다./,
        (match) => `消耗 AP 自 ${match[1]} 增加為 ${match[2]}。`],
    [/AP 소모량이 (\d+)에서 (\d+)로 감소됩니다./,
        (match) => `消耗 AP 自 ${match[1]} 減少為 ${match[2]}。`],
    [/AP 소모량이 (\d+)에서 (\d+)으로 증가됩니다./,
        (match) => `消耗 AP 自 ${match[1]} 增加為 ${match[2]}。`],
    [/AP 소모량이 (\d+)에서 (\d+)으로 감소됩니다./,
        (match) => `消耗 AP 自 ${match[1]} 減少為 ${match[2]}。`],

    [/(\d+)레벨 기준 기본 공격력이 (ATK|MATK) (\d+)%에서 (ATK|MATK) (\d+)%로 증가됩니다./,
        (match) => `以等級 ${match[1]} 為基準，技能倍率自 ${match[2]} ${match[3]}%，增加為 ${match[4]} ${match[5]}%。`],
    [/공격 회수가 기존 (\d+)회에서 (\d+)회로 증가됩니다./,
        (match) => `打擊次數自 ${match[2]} 次，增加為 ${match[3]} 次。`],
    
    [/(\d+)레벨 기준 기본 공격력이/,
        (match) => `以等級 ${match[1]} 為基準，技能倍率自 `],
    [/(\d+)레벨 기준 기본 1회당 공격력이/,
        (match) => `以等級 ${match[1]} 為基準，每 1 次打擊的技能倍率自 `],
]);
